Additional scripts may be added at any time.

# WEFAX.py

**WEFAX.py** encodes and decodes **WEFAX (Weather Facsimile)** transmissions.
Functions:
1. **Encode BMP → WEFAX WAV**  
   Convert greyscale `.BMP` image files into WEFAX-compatible `.WAV` audio transmissions.
2. **Encode TXT → BMP → WEFAX WAV**  
   Convert plain text `.TXT` files into greyscale `.BMP` images, then encode those images into WEFAX `.WAV` files.
3. **Decode WEFAX WAV → BMP**  
   Decode received WEFAX `.wav` audio signals into greyscale `.BMP` images.
